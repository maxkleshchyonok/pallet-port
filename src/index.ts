import App from './pages/app';
import './index.scss';

const app = new App();
app.run();

// alert('Не отображаются ещё данные в ЛК, а так всё работает ) Вопросы кидайте в чат справа внизу, ответим в момент');
